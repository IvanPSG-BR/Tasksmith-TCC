/**
 * Tasksmith - JavaScript principal
 * Implementação simples sem frameworks
 */

document.addEventListener('DOMContentLoaded', function() {
    console.log('Tasksmith carregado!');

    // Expor funções globalmente
    window.tasksmith = {
        showMessage
    };
});
